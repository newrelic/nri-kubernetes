# Infrastructure agent configuration for sidecar mode
# Enables HTTP server for nri-kubernetes to send metrics

# Enable HTTP server to receive metrics from nri-kubernetes
http_server_enabled: true
http_server_port: 8001

# Disable features not needed in sidecar mode
is_secure_forward_only: true

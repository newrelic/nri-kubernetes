{{- if .Values.loadBalancerService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-lb
  {{- with .Values.loadBalancerService.annotations }}
  annotations:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
spec:
  type: LoadBalancer
  {{- if .Values.loadBalancerService.fakeIP.enabled }}
  loadBalancerIP: {{ .Values.loadBalancerService.fakeIP.value | quote }}
  {{- end }}
  externalName: newrelic.com
  ports:
    - port: 80
  {{- end }}
